replicaCount: 1

image:
  repository: ghcr.io/rionchoi/platform-web-main
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

ingress:
  enabled: false
  host: app.example.com
  tls:
    enabled: false
    clusterIssuer: letsencrypt-prod
  rateLimit:
    enabled: false
    rps: "20"
    connections: "20"
    sensitiveRps: "3"
    sensitiveConnections: "5"
    sensitivePaths: []

resources:
  requests:
    memory: "256Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
    cpu: "500m"

env:
  NODE_ENV: production
  NEXTAUTH_URL: http://localhost:3000

secret:
  name: web-main-secret
